services:
  neo4j-final-graph:
    image: neo4j:latest
    container_name: neo4j-final-graph
    ports:
      - "${FINAL_GRAPH_DESKTOP_PORT}:7474"
      - "${FINAL_GRAPH_BOLT_PORT}:7687"
    volumes:
      - ${FINAL_GRAPH_DIR}/data:/data
      - ${FINAL_GRAPH_DIR}/logs:/logs
      - ${FINAL_GRAPH_DIR}/conf:/conf
      - ${FINAL_GRAPH_DIR}/import:/import
    environment:
      - NEO4J_AUTH=none
      - NEO4J_PLUGINS=["apoc"]
    healthcheck:
      test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider localhost:7474 || exit 1"]
      interval: 10s
      timeout: 10s
      retries: 5
      start_period: 40s
