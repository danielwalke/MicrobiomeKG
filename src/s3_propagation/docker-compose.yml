services:
  neo4j-propagation-graph:
    image: neo4j:latest
    container_name: neo4j-propagation-graph
    ports:
      - "${PROPAGATED_GRAPH_DESKTOP_PORT}:7474"
      - "${PROPAGATED_GRAPH_BOLT_PORT}:7687"
    volumes:
      - ${PROPAGATED_GRAPH_DIR}/data:/data
      - ${PROPAGATED_GRAPH_DIR}/logs:/logs
      - ${PROPAGATED_GRAPH_DIR}/conf:/conf
      - ${PROPAGATED_GRAPH_DIR}/import:/import
    environment:
      - NEO4J_AUTH=none
      - NEO4J_PLUGINS=["apoc"]
    healthcheck:
      test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider localhost:7474 || exit 1"]
      interval: 10s
      timeout: 10s
      retries: 5
      start_period: 40s
